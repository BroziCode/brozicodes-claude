{
  "mcpServers": {
    "brozicodes-claude": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/server/dist/index.js"],
      "env": {
        "BROZICODE_ROOT": "${CLAUDE_PLUGIN_ROOT}"
      }
    }
  }
}
